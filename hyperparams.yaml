data:
  raw_path: "data/raw/dataset.csv"
  processed_path: "data/processed/features.csv"
  test_size: 0.2
  random_state: 42

model:
  type: "LogisticRegression"
  params:
    C: 1.0
    solver: "lbfgs"
    max_iter: 100

training:
  experiment_name: "mlops_cicd_experiment"
  run_name: "pipeline_run"
